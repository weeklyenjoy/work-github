<div class="container">
  <div class="text-danger">
  <% if @genre.errors.any? %>
     <span class="h4"><%= @genre.errors.count %>errors prohibited this obj from being saved:</span>
    <% @genre.errors.full_messages.each do |message| %><br>
       ・<%= message %>
    <% end %>
  <% end %>
  </div>
  <h3 class="head-line mb-5">ジャンル編集</h3>
  <%= form_with model: @genre, url: admin_genre_path, local: true do |f| %>
  <p>ジャンル名 <span class="ml-4"><%= f.text_field :name %></span> <span class="ml-4"><%= f.submit '変更を保存', class: "btn btn-outline-primary" %></span></p>
  <% end %>
</div>